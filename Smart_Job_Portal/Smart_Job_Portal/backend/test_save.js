const axios = require('axios');

async function testSave() {
    try {
        const token = 'YOUR_TOKEN'; // I don't have a token easily.
        // Instead of axios, let's just use the db pool in a script.
    } catch (err) { }
}
